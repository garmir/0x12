name: Unique Test - 0x12
on: [push, workflow_dispatch]

jobs:
  unique-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: DeterminateSystems/nix-installer-action@main
        continue-on-error: true

      - name: Unique Test Execution - 0x12
        continue-on-error: true
        run: |
          nix-shell -p curl jq coreutils --run '
            echo "=== Unique Test: 0x12 ===" > unique-test-0x12.txt
            echo "Repository: ${{ github.repository }}" >> unique-test-0x12.txt
            echo "Test ID: TEST_048" >> unique-test-0x12.txt
            echo "Timestamp: $(date)" >> unique-test-0x12.txt
            curl -s ifconfig.me >> unique-test-0x12.txt
            echo "Unique instruction: Hex node analysis and coordination testing" >> unique-test-0x12.txt
          '

      - name: Ubuntu Fallback - 0x12
        if: failure()
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y curl jq
          echo "Ubuntu fallback for 0x12" > fallback-0x12.txt
          curl -s ifconfig.me >> fallback-0x12.txt

      - name: Upload Results
        uses: actions/upload-artifact@v4
        with:
          name: unique-test-0x12
          path: "*.txt"
          retention-days: 2